# Lahore Pulse AI — Backend Dependencies
# Phase 0: Foundation only
# Generated from installed versions — do not install unneeded packages.

# Web framework
fastapi==0.141.1
uvicorn==0.52.1
starlette==1.4.1

# Data validation & settings
pydantic==2.13.4
pydantic-settings==2.14.2

# HTTP client (for future data provider integration)
httpx==0.28.1

# Environment management
python-dotenv==1.2.2

# Logging
loguru==0.7.3

# Data processing (Phase 2 — dataset building)
numpy>=2.0.0
pandas>=2.0.0

# Machine learning (Phase 4 — model training & evaluation)
scikit-learn>=1.4.0
joblib>=1.3.0

# JSON (high-performance, optional)
orjson==3.11.9
